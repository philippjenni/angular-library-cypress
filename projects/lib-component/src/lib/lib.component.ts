import { Component, EventEmitter, Output } from '@angular/core';
import { LibBase } from 'lib-base';

@Component({
  selector: 'lib-libcomponent',
  templateUrl: './lib.component.html',
})
export class LibComponent extends LibBase {

}
